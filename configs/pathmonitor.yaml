log:
    # debug | info | warning | error
    level: debug
    # file://<path> | syslog
    output: file:///tmp/media-monitor.log
paths:
    - path: /media/files
      filter: .*.txt
      command: ls -l {}

